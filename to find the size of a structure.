#include <stdio.h>

    char name[50];  
    int age;       
    float grade;   
};
int main()
{
    struct Student student;
    size_t size = sizeof(student);
    printf("Size of the structure 'Student': %zu bytes\n", size);
    return 0;
}
