#include <stdio.h>

union Data 
{
    int intValue;      
    float floatValue;  
    char strValue[50];  
};

int main()
{
   
    union Data data;

    size_t size = sizeof(data);

    printf("Size of the union 'Data': %zu bytes\n", size);

    return 0;
}
